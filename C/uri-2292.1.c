#include <stdio.h>
#include <string.h>

int main ()
{
    char s[65];
    unsigned long long swi, t, i, j, k, tam, mudou;
    
    memset(s, '\0', 65);
    scanf("%llu", t);
    getchar();
    
    for(i = 0;i < t;i++)
    {
        scanf("%s %llu", s, &swi);
        tam = strlen(s);
        
        if(swi % 2 == 0)
        {
            
        }
    }
}
